{
  "name": "sandbox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:sass": "sass src/assets/sass:src/assets/_css",
    "watch:sass": "sass --watch src/assets/sass:src/assets/_css",
    "build:eleventy": "npx eleventy",
    "watch:eleventy": "npx eleventy --serve --port=8081",
    "start": "npm-run-all build:sass --parallel watch:*",
    "build": "npm-run-all build:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-navigation": "^0.3.5",
    "@11ty/eleventy-plugin-directory-output": "^1.0.1",
    "npm-run-all": "^4.1.5",
    "sass": "^1.83.0"
  }
}
